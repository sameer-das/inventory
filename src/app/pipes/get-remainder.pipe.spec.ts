import { GetRemainderPipe } from './get-remainder.pipe';

describe('GetRemainderPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRemainderPipe();
    expect(pipe).toBeTruthy();
  });
});
